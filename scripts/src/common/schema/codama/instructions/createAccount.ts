/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getStructDecoder,
  getStructEncoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { REGISTRY_CPI_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';
import {
  getUint32Decoder,
  getUint32Encoder,
  type Uint32,
  type Uint32Args,
} from '../types';

export const CREATE_ACCOUNT_DISCRIMINATOR = 4;

export function getCreateAccountDiscriminatorBytes() {
  return getU8Encoder().encode(CREATE_ACCOUNT_DISCRIMINATOR);
}

export type CreateAccountInstruction<
  TProgram extends string = typeof REGISTRY_CPI_PROGRAM_ADDRESS,
  TAccountSystemProgram extends string | AccountMeta<string> = string,
  TAccountSender extends string | AccountMeta<string> = string,
  TAccountBump extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountUserCounter extends string | AccountMeta<string> = string,
  TAccountUserId extends string | AccountMeta<string> = string,
  TAccountUserAccount extends string | AccountMeta<string> = string,
  TAccountUserRotationState extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      TAccountSender extends string
        ? WritableSignerAccount<TAccountSender> &
            AccountSignerMeta<TAccountSender>
        : TAccountSender,
      TAccountBump extends string
        ? ReadonlyAccount<TAccountBump>
        : TAccountBump,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountUserCounter extends string
        ? WritableAccount<TAccountUserCounter>
        : TAccountUserCounter,
      TAccountUserId extends string
        ? WritableAccount<TAccountUserId>
        : TAccountUserId,
      TAccountUserAccount extends string
        ? WritableAccount<TAccountUserAccount>
        : TAccountUserAccount,
      TAccountUserRotationState extends string
        ? WritableAccount<TAccountUserRotationState>
        : TAccountUserRotationState,
      ...TRemainingAccounts,
    ]
  >;

export type CreateAccountInstructionData = {
  discriminator: number;
  maxDataSize: Uint32;
};

export type CreateAccountInstructionDataArgs = { maxDataSize: Uint32Args };

export function getCreateAccountInstructionDataEncoder(): Encoder<CreateAccountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ['discriminator', getU8Encoder()],
      ['maxDataSize', getUint32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_ACCOUNT_DISCRIMINATOR })
  );
}

export function getCreateAccountInstructionDataDecoder(): Decoder<CreateAccountInstructionData> {
  return getStructDecoder([
    ['discriminator', getU8Decoder()],
    ['maxDataSize', getUint32Decoder()],
  ]);
}

export function getCreateAccountInstructionDataCodec(): Codec<
  CreateAccountInstructionDataArgs,
  CreateAccountInstructionData
> {
  return combineCodec(
    getCreateAccountInstructionDataEncoder(),
    getCreateAccountInstructionDataDecoder()
  );
}

export type CreateAccountInput<
  TAccountSystemProgram extends string = string,
  TAccountSender extends string = string,
  TAccountBump extends string = string,
  TAccountConfig extends string = string,
  TAccountUserCounter extends string = string,
  TAccountUserId extends string = string,
  TAccountUserAccount extends string = string,
  TAccountUserRotationState extends string = string,
> = {
  systemProgram: Address<TAccountSystemProgram>;
  sender: TransactionSigner<TAccountSender>;
  bump: Address<TAccountBump>;
  config: Address<TAccountConfig>;
  userCounter: Address<TAccountUserCounter>;
  userId: Address<TAccountUserId>;
  userAccount: Address<TAccountUserAccount>;
  userRotationState: Address<TAccountUserRotationState>;
  maxDataSize: CreateAccountInstructionDataArgs['maxDataSize'];
};

export function getCreateAccountInstruction<
  TAccountSystemProgram extends string,
  TAccountSender extends string,
  TAccountBump extends string,
  TAccountConfig extends string,
  TAccountUserCounter extends string,
  TAccountUserId extends string,
  TAccountUserAccount extends string,
  TAccountUserRotationState extends string,
  TProgramAddress extends Address = typeof REGISTRY_CPI_PROGRAM_ADDRESS,
>(
  input: CreateAccountInput<
    TAccountSystemProgram,
    TAccountSender,
    TAccountBump,
    TAccountConfig,
    TAccountUserCounter,
    TAccountUserId,
    TAccountUserAccount,
    TAccountUserRotationState
  >,
  config?: { programAddress?: TProgramAddress }
): CreateAccountInstruction<
  TProgramAddress,
  TAccountSystemProgram,
  TAccountSender,
  TAccountBump,
  TAccountConfig,
  TAccountUserCounter,
  TAccountUserId,
  TAccountUserAccount,
  TAccountUserRotationState
> {
  // Program address.
  const programAddress = config?.programAddress ?? REGISTRY_CPI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    sender: { value: input.sender ?? null, isWritable: true },
    bump: { value: input.bump ?? null, isWritable: false },
    config: { value: input.config ?? null, isWritable: false },
    userCounter: { value: input.userCounter ?? null, isWritable: true },
    userId: { value: input.userId ?? null, isWritable: true },
    userAccount: { value: input.userAccount ?? null, isWritable: true },
    userRotationState: {
      value: input.userRotationState ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.systemProgram),
      getAccountMeta(accounts.sender),
      getAccountMeta(accounts.bump),
      getAccountMeta(accounts.config),
      getAccountMeta(accounts.userCounter),
      getAccountMeta(accounts.userId),
      getAccountMeta(accounts.userAccount),
      getAccountMeta(accounts.userRotationState),
    ],
    data: getCreateAccountInstructionDataEncoder().encode(
      args as CreateAccountInstructionDataArgs
    ),
    programAddress,
  } as CreateAccountInstruction<
    TProgramAddress,
    TAccountSystemProgram,
    TAccountSender,
    TAccountBump,
    TAccountConfig,
    TAccountUserCounter,
    TAccountUserId,
    TAccountUserAccount,
    TAccountUserRotationState
  >);
}

export type ParsedCreateAccountInstruction<
  TProgram extends string = typeof REGISTRY_CPI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    systemProgram: TAccountMetas[0];
    sender: TAccountMetas[1];
    bump: TAccountMetas[2];
    config: TAccountMetas[3];
    userCounter: TAccountMetas[4];
    userId: TAccountMetas[5];
    userAccount: TAccountMetas[6];
    userRotationState: TAccountMetas[7];
  };
  data: CreateAccountInstructionData;
};

export function parseCreateAccountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCreateAccountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      systemProgram: getNextAccount(),
      sender: getNextAccount(),
      bump: getNextAccount(),
      config: getNextAccount(),
      userCounter: getNextAccount(),
      userId: getNextAccount(),
      userAccount: getNextAccount(),
      userRotationState: getNextAccount(),
    },
    data: getCreateAccountInstructionDataDecoder().decode(instruction.data),
  };
}
